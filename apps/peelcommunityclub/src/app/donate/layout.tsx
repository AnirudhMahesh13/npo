export const metadata = {
  title: 'PeelCommunityClub - Donate',
  description: 'Support PeelCommunityClub with your generous donations.',
};

export default function DonateLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
