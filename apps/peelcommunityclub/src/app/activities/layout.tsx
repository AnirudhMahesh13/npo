export const metadata = {
  title: 'PeelCommunityClub - Activities',
  description: 'Sign up for volunteer activities with PeelCommunityClub.',
};

export default function ActivitiesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
